<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OpsT360.Views.SealInspectionPage"
             Title="RFID Seals Activation"
             BackgroundColor="#F3F5FB">
    <ScrollView>
        <VerticalStackLayout Padding="16,18,16,28" Spacing="14">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                <Border BackgroundColor="#24379E" StrokeThickness="0" StrokeShape="RoundRectangle 12" Padding="10">
                    <Label Text="RFID" TextColor="White" FontAttributes="Bold" FontSize="12" />
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="OPS T360" FontSize="12" TextColor="#6E7891" FontAttributes="Bold" />
                    <Label Text="RFID Seals Activation" FontSize="24" FontAttributes="Bold" TextColor="#1C2537" />
                </VerticalStackLayout>
            </Grid>

            <Border BackgroundColor="White" Stroke="#E2E8F3" StrokeShape="RoundRectangle 16" Padding="14">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Container information" FontAttributes="Bold" TextColor="#8A93A6" />
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Container Id" TextColor="#8A93A6" FontSize="12" />
                        <Entry Placeholder="Ej: HSU3523430"
                               Text="{Binding ContainerId}"
                               BackgroundColor="#F1F4FA"
                               TextColor="#243048"
                               HeightRequest="46" />
                    </VerticalStackLayout>

                    <CollectionView ItemsSource="{Binding ContainerSuggestions}"
                                    HeightRequest="120"
                                    SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,6" BackgroundColor="#EEF3FF" Stroke="#D7E2FF" StrokeShape="RoundRectangle 10" Padding="10,8">
                                    <Label Text="{Binding .}" TextColor="#24379E" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectContainerCommand}"
                                                                  CommandParameter="{Binding .}" />
                                        </Label.GestureRecognizers>
                                    </Label>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="White" Stroke="#E2E8F3" StrokeShape="RoundRectangle 16" Padding="14">
                <VerticalStackLayout Spacing="10">
                    <Label Text="RFID seal registry" FontAttributes="Bold" TextColor="#8A93A6" />
                    <Label Text="Pulsa Start antenna, luego Read seal y gatilla el hand-held para leer el sticker azul RFID."
                           FontSize="12"
                           TextColor="#6E7891" />
                    <Button Text="Start antenna"
                            Command="{Binding StartAntennaAsyncCommand}"
                            BackgroundColor="#24379E"
                            TextColor="White" />

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Seal #1" TextColor="#8A93A6" FontSize="12" />
                        <Entry x:Name="SealEntry1" Placeholder="Capture seal #1" Text="{Binding Seals[0].Code}" IsReadOnly="{Binding Seals[0].IsLocked}" Completed="OnSealCompleted" ClassId="1" BackgroundColor="#F1F4FA" HeightRequest="46" TextColor="#243048" Focused="OnSealEntryFocused"/>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Text="Read seal" ClassId="1" Clicked="OnReadSealClicked" BackgroundColor="#4357E8" />
                            <Button Grid.Column="1" Text="Image" Command="{Binding LoadSealImageAsyncCommand}" CommandParameter="1" IsEnabled="{Binding CanUploadImages}" BackgroundColor="#24379E" />
                        </Grid>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Seal #2" TextColor="#8A93A6" FontSize="12" />
                        <Entry x:Name="SealEntry2" Placeholder="Capture seal #2" Text="{Binding Seals[1].Code}" IsReadOnly="{Binding Seals[1].IsLocked}" Completed="OnSealCompleted" ClassId="2" BackgroundColor="#F1F4FA" HeightRequest="46" TextColor="#243048" Focused="OnSealEntryFocused"/>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Text="Read seal" ClassId="2" Clicked="OnReadSealClicked" BackgroundColor="#4357E8" />
                            <Button Grid.Column="1" Text="Image" Command="{Binding LoadSealImageAsyncCommand}" CommandParameter="2" IsEnabled="{Binding CanUploadImages}" BackgroundColor="#24379E" />
                        </Grid>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Seal #3" TextColor="#8A93A6" FontSize="12" />
                        <Entry x:Name="SealEntry3" Placeholder="Capture seal #3" Text="{Binding Seals[2].Code}" IsReadOnly="{Binding Seals[2].IsLocked}" Completed="OnSealCompleted" ClassId="3" BackgroundColor="#F1F4FA" HeightRequest="46" TextColor="#243048" Focused="OnSealEntryFocused"/>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Text="Read seal" ClassId="3" Clicked="OnReadSealClicked" BackgroundColor="#4357E8" />
                            <Button Grid.Column="1" Text="Image" Command="{Binding LoadSealImageAsyncCommand}" CommandParameter="3" IsEnabled="{Binding CanUploadImages}" BackgroundColor="#24379E" />
                        </Grid>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Seal #4" TextColor="#8A93A6" FontSize="12" />
                        <Entry x:Name="SealEntry4" Placeholder="Capture seal #4" Text="{Binding Seals[3].Code}" IsReadOnly="{Binding Seals[3].IsLocked}" Completed="OnSealCompleted" ClassId="4" BackgroundColor="#F1F4FA" HeightRequest="46" TextColor="#243048" Focused="OnSealEntryFocused"/>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Text="Read seal" ClassId="4" Clicked="OnReadSealClicked" BackgroundColor="#4357E8" />
                            <Button Grid.Column="1" Text="Image" Command="{Binding LoadSealImageAsyncCommand}" CommandParameter="4" IsEnabled="{Binding CanUploadImages}" BackgroundColor="#24379E" />
                        </Grid>
                    </VerticalStackLayout>

                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8" Margin="0,6,0,0">
                        <Label Text="Container image" VerticalTextAlignment="Center" TextColor="#8A93A6" FontAttributes="Bold"/>
                        <Button Grid.Column="1" Text="Upload" Command="{Binding LoadContainerImageAsyncCommand}" IsEnabled="{Binding CanUploadImages}" BackgroundColor="#24379E" WidthRequest="90" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="White" Stroke="#E2E8F3" StrokeShape="RoundRectangle 16" Padding="14">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Status" FontAttributes="Bold" TextColor="#8A93A6" />
                    <Label Text="{Binding StatusText}" TextColor="#4C5B76" />

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Text="Editar / Reemplazar"
                                BackgroundColor="#6D7A92"
                                Command="{Binding ToggleEditModeCommand}" />
                        <Button Grid.Column="1"
                                Text="Send"
                                BackgroundColor="#11192C"
                                IsEnabled="{Binding CanSend}"
                                Command="{Binding SendAsyncCommand}" />
                    </Grid>

                    <Button Text="Cancel"
                            BackgroundColor="#EBEEF5"
                            TextColor="#22304A"
                            Command="{Binding CancelCommand}" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
